# This is a basic workflow to help you get started with Actions
name: Test variables

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        default: TEST
        options:
          - TEST
          - PROD

jobs:
  test:
    runs-on: ubuntu-latest
  
    steps:
    - name: Setup TEST variables
      if: ${{ github.inputs.environment }} == TEST
      run: |
        echo "FTP_SERVER=$(echo ${{ secrets.FTP_SERVER_TEST }})" >> $GITHUB_ENV
  
    - name: Setup PROD variables
      if: ${{ github.inputs.environment }} == TEST
      run: |
        echo "FTP_SERVER=$(echo ${{ secrets.FTP_SERVER_PROD }})" >> $GITHUB_ENV

      # Runs a set of commands using the runners shell
    - name: Run a multi-line script
      run: |
        echo il valore è ${{ env.FTP_SERVER }}
